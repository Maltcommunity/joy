declare interface Window {
    /* eslint-disable camelcase */
    __malt_cdn_url: string;
    /* eslint-disable camelcase */
    __svg_sprite_url: string;
}

interface Tab {
    selectedTab: string;
    href: string;
}
